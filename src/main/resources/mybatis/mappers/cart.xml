<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CartXml">

	<!-- 리스트 불러오기 -->
	<select id="selectList" resultType="com.javaex.vo.CartVo">
		SELECT c.CART_NO , w.WINE_NAME, wd.WINE_PRICE 
		  FROM CART c ,WINE w , WINE_DESCRIPTION wd 
         WHERE c.WINE_NO = w.WINE_NO
           AND w.WINE_NO = wd.WINE_NO
	</select>

	
</mapper>
